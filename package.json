{
  "name": "jetstream-pg-writer",
  "private": true,
  "type": "module",
  "scripts": {
    "dev:producer": "pnpm --filter @jetstream-pg-writer/producer dev",
    "dev:consumer": "pnpm --filter @jetstream-pg-writer/consumer dev",
    "dev:reader": "pnpm --filter @jetstream-pg-writer/reader dev",
    "dev:frontend": "pnpm --filter @jetstream-pg-writer/frontend dev"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}