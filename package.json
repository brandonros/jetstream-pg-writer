{
  "name": "jetstream-pg-writer",
  "private": true,
  "type": "module",
  "scripts": {
    "dev:write-gateway": "pnpm --filter @jetstream-pg-writer/write-gateway dev",
    "dev:write-processor": "pnpm --filter @jetstream-pg-writer/write-processor dev",
    "dev:read-api": "pnpm --filter @jetstream-pg-writer/read-api dev",
    "dev:frontend": "pnpm --filter @jetstream-pg-writer/frontend dev"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}